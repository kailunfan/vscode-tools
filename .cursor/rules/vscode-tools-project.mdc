---
description: vscode_tools 扩展目标、目录约定与新增工具流程
alwaysApply: true
---

# vscode_tools 项目规则

## 目标

VS Code / Cursor 扩展：**集成常用开发工具**。每个工具独立实现，由注册表统一挂载。

## 目录约定

- `src/extension.ts`：仅 `activate` / `deactivate`，调用 `registerAllTools`。
- `src/tools/registry.ts`：`registerAllTools(context)`，在此处 `import` 并调用各工具的 `registerXxxTool`。
- `src/tools/<工具名>/`
  - `index.ts`：注册命令、Hover、菜单等；导出命令 ID 常量（与 `package.json` 的 `contributes.commands` **字符串一致**）。
  - `model.ts`（或同类）：**纯逻辑**，不 `import vscode`，便于复用与测试。

新增工具时：新建 `src/tools/<名>/`，实现 `registerXxxTool`，在 `registry.ts` 里追加一行，并更新 `package.json` 的 `contributes`（commands、menus、activationEvents 等）。

## 实现注意

- `Disposable` 一律 `context.subscriptions.push(...)`。
- 改动保持最小范围；不要顺带大重构无关模块。
- 编译输出在 `out/`，勿手改；改源码后执行 `npm run compile`。

## 时间戳工具（现有行为摘要）

- 解析：**秒级**整数部分 1～11 位（可带小数）；**毫秒级** 12～13 位整数；≥14 位或非法组合不解析。
- 展示：仅**本地时间**，格式 `YYYY-MM-DD HH:mm:ss`。
- 光标扩词：同一行内向左右扩展，字符集为数字与 `,`、`_`、`.`；**遇空格停止**，不跨空格（手动选区仍可由 `model` 去掉空格后解析）。
